<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://java.sun.com/xml/ns/javaee" xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd" id="WebApp_ID" version="3.0">
  
  <display-name>Asta Online</display-name>

	<!-- ========== CONTEXT PARAMETERS ===================================== -->
	<!--
        Location of the XML file that defines the root application context.
        Applied by ContextLoaderServlet.
    -->
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>
			/WEB-INF/spring-beans.xml
			/WEB-INF/spring-security.xml
		</param-value>
	</context-param>
	<!--
        Whether the web app root system property should be exposed, allowing for
        log file paths relative to the web application root directory. Default is
        "true"; specify "false" to suppress expose of the web app root system property. 
    -->
	<!-- context-param>
		<param-name>log4jExposeWebAppRoot</param-name>
		<param-value>false</param-value>
	</context-param -->
	
	<!-- ========== LISTENERS ============================================== -->
    <!--
        Listener that flushes the JDK's JavaBeans Introspector  cache on webapp
        shutdown.
    -->
    <listener>
        <listener-class>org.springframework.web.util.IntrospectorCleanupListener</listener-class>
    </listener>
    <!--
        Loads the root application context of this web app at startup, by
        default from "/WEB-INF/applicationContext.xml".
        Note that you need to fall back to Spring's ContextLoaderServlet for
        J2EE servers that do not follow the Servlet 2.4 initialization order.
    -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>
    <!--
        Listener that exposes the request to the current thread.
    -->
	<listener>
		<listener-class>org.springframework.web.context.request.RequestContextListener</listener-class>
	</listener>
	<!--
        Listener that startup/shutdown all the necessary application components.
    -->
	<listener>
		<listener-class>it.gab.asta.web.context.WebServletListener</listener-class>
	</listener>
	
	
    <!-- ========== FILTERS ================================================ -->
	<filter>
		<filter-name>requestFilter</filter-name>
		<filter-class>it.advalso.gpd.web.context.WebRequestFilter</filter-class>
	</filter>
	<filter-mapping>
		<filter-name>requestFilter</filter-name>
		<servlet-name>flow</servlet-name>
	</filter-mapping>
	<filter-mapping>
		<filter-name>requestFilter</filter-name>
		<servlet-name>spring</servlet-name>
	</filter-mapping>
	
	<filter>
    	<filter-name>springSecurityFilterChain</filter-name>
    	<filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
	</filter>
	<filter-mapping>
  		<filter-name>springSecurityFilterChain</filter-name>
  		<url-pattern>/security/*</url-pattern>
	</filter-mapping>
	<filter-mapping>
  		<filter-name>springSecurityFilterChain</filter-name>
  		<servlet-name>flow</servlet-name>
	</filter-mapping>
	<filter-mapping>
  		<filter-name>springSecurityFilterChain</filter-name>
  		<servlet-name>dwr</servlet-name>
	</filter-mapping>
	
	<filter-mapping>
		<filter-name>requestFilter</filter-name>
		<servlet-name>document</servlet-name>
	</filter-mapping>
	
	<filter>
		<filter-name>jerichoHtmlFilter</filter-name>
		<filter-class>it.advalso.gpd.support.jericho.JerichoHtmlFilter</filter-class>
	</filter>
	<filter-mapping>
  		<filter-name>jerichoHtmlFilter</filter-name>
  		<servlet-name>flow</servlet-name>
	</filter-mapping>
	
	<!-- ========== SERVLETS =============================================== -->
	<!-- Apache-Axis2 Servlet -->
	<!-- servlet>
		<display-name>Apache-Axis Servlet</display-name>
        <servlet-name>AxisServlet</servlet-name>
        <servlet-class>org.apache.axis2.transport.http.AxisServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
	    <servlet-name>AxisServlet</servlet-name>
	    <url-pattern>/services/*</url-pattern>
	</servlet-mapping -->
	
	<!-- DWR 2 Spring Servlet -->
	<servlet>
		<display-name>DWR Servlet</display-name>
		<servlet-name>dwr</servlet-name>
  		<servlet-class>org.directwebremoting.spring.DwrSpringServlet</servlet-class>
  		<init-param>
     		<param-name>debug</param-name>
     		<param-value>true</param-value>
  		</init-param>
	</servlet>
	<servlet-mapping>
		<servlet-name>dwr</servlet-name>
		<url-pattern>/dwr/*</url-pattern>
	</servlet-mapping>
	
	<!-- Eastwood Charts Servlet -->
	<servlet>
		<display-name>Eastwood Charts</display-name>
		<servlet-name>eastwood</servlet-name>
  		<servlet-class>org.jfree.eastwood.ChartServlet</servlet-class>
	</servlet>
	<servlet-mapping>
		<servlet-name>eastwood</servlet-name>
		<url-pattern>/eastwood</url-pattern>
	</servlet-mapping>
	
	<!-- Document KFILE Servlet -->
	
	<servlet>
		<servlet-name>document</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/spring/document-servlet.xml</param-value>
		</init-param>
	</servlet>
	<servlet-mapping>
		<servlet-name>document</servlet-name>
		<url-pattern>/document/*</url-pattern>
	</servlet-mapping>
	<!-- Spring WebFlow Servlet -->
	<servlet>
	<servlet-name>flow</servlet-name>
	<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/flow-servlet.xml</param-value>
		</init-param>
	</servlet>
	<servlet-mapping>
	<servlet-name>flow</servlet-name>
		<url-pattern>*.do</url-pattern>
	</servlet-mapping>
		
	<!-- Spring MVC Servlet -->
	<servlet>
	<servlet-name>spring</servlet-name>
	<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/spring-servlet.xml</param-value>
		</init-param>
	</servlet>
	<servlet-mapping>
		<servlet-name>spring</servlet-name>
		<url-pattern>/chart/*</url-pattern>
	</servlet-mapping>
	<servlet-mapping>
		<servlet-name>spring</servlet-name>
		<url-pattern>/email/*</url-pattern>
	</servlet-mapping>
	<servlet-mapping>
		<servlet-name>spring</servlet-name>
		<url-pattern>/report/*</url-pattern>
	</servlet-mapping>
	
	<!-- Export Servlet -->
	<servlet>
		<servlet-name>export</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/spring/export-servlet.xml</param-value>
		</init-param>
	</servlet>
	<servlet-mapping>
		<servlet-name>export</servlet-name>
		<url-pattern>/export/*</url-pattern>
	</servlet-mapping>
	
	<!-- ========== RESOURCES ===================================== -->
	<resource-ref>
		<description>DataSource per applicazione gpd</description>
 		<res-ref-name>jdbc/appdb</res-ref-name>
		<res-type>javax.sql.DataSource</res-type>
		<res-auth>Container</res-auth>
  	</resource-ref>

	<!-- ========== WEB SETUP ===================================== -->
	<mime-mapping>
		<extension>html</extension>
		<mime-type>text/html</mime-type>
	</mime-mapping>
	<mime-mapping>
		<extension>txt</extension>
		<mime-type>text/plain</mime-type>
	</mime-mapping>
	
	<session-config>
		<session-timeout>30</session-timeout>
	</session-config>

	<welcome-file-list>
		<welcome-file>welcome.jsp</welcome-file>
	</welcome-file-list>

</web-app>